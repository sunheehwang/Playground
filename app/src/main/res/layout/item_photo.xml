<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white">

    <ImageView
            android:id="@+id/image_view"
            android:layout_width="100dp"
            android:layout_height="100dp"/>

    <TextView
            android:id="@+id/text_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/image_view"
            android:layout_marginStart="10dp"
            app:layout_goneMarginStart="0dp"
            tools:text="title"/>

    <TextView
            android:id="@+id/text_photo_size"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/image_view"
            app:layout_constraintTop_toBottomOf="@id/text_title"
            android:layout_marginStart="10dp"
            app:layout_goneMarginStart="0dp"
            tools:text="photo_size"/>

    <TextView
            android:id="@+id/text_photo_url"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/image_view"
            app:layout_constraintTop_toBottomOf="@id/text_photo_size"
            android:layout_marginStart="10dp"
            app:layout_goneMarginStart="0dp"
            tools:text="photo_url"
            android:autoLink="all"/>

    <TextView
            android:id="@+id/text_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/image_view"
            app:layout_constraintTop_toBottomOf="@id/text_photo_url"
            android:layout_marginStart="10dp"
            app:layout_goneMarginStart="0dp"
            tools:text="date"/>

</androidx.constraintlayout.widget.ConstraintLayout>